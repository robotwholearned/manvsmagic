---
pageable: true
per_page: 1
---
<% page_articles.each_with_index do |article, i| %>
<h1><%= article.title %>  <span> - <%= article.date.strftime('%b %e, %Y') %></span></h1>
<div class="pagination">
<% if paginate && num_pages > 1 %>
  <%= link_to 'Previous', next_page %>
  <%= link_to 'Next', prev_page %>
<% end %>
</div>
<%= article.body %>
<% end %>

<div class="pagination">
<% if paginate && num_pages > 1 %>
    <%= link_to 'Previous', next_page %>
    <%= link_to 'Next', prev_page%>
<% end %>
</div>